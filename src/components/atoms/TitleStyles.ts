import styled from 'styled-components'

/**
 * Componente estilizado que representa un título en formato de texto.
 * Utiliza una etiqueta `span` con peso de fuente negrita y tamaño de fuente de 30px.
 *
 * @remarks
 * Este componente está diseñado para ser utilizado como
 * encabezado o título destacado en la interfaz de usuario.
 */
export const TitleStyles = styled.span`
  font-weight: 700;
  font-size: 30px;
`
