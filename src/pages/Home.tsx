import { HomeTemplate } from '@/components'

/**
 * Componente principal de la página de inicio.
 *
 * Renderiza el componente `HomeTemplate` que contiene la
 * estructura y contenido principal de la página de inicio de la aplicación.
 *
 * @component
 */
const Home: React.FC = () => {
  return (
    <HomeTemplate />
  )
}

export default Home
